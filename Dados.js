// dados.js

// Array de objetos que armazenará os livros
const bancoDeDados = {
    livros: []
};

// Exportamos o objeto para que outros arquivos possam acessá-lo e modificá-lo
module.exports = bancoDeDados;
